#include <cmath>
#include <vector>
#include <string>

double funkcja(double x, char wybor);
std::string funkcja_str(char wybor);

double horner(double x, double tablica[], int n);
double silnia(int k);
double potega(double n, int k);
